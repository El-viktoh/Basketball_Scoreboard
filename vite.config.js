/*import {defineConfig} from "vite"

export default defineConfig({
	plugins: [
		
	]
})*/

import { defineConfig } from 'vite';

export default defineConfig({
  base: '/', // or './' for relative paths
});
